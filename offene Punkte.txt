 1. Score-Trend — Aktuell ist der Score ein Snapshot. Wenn man sieht dass er von 45 → 55 → 68 über 3 Wochen gestiegen ist, ist das viel actionabler als eine einzelne Zahl.
  Scores historisch in Supabase speichern und als Mini-Chart darstellen.
  2. Tägliches Morgen-Briefing — Eine KI-generierte Karte um ~8:00 Uhr: Markt-Übernacht-Bewegungen, relevante News für deine Watchlist, Top-Chance des Tages. Würde die App
  zum täglichen Ritual machen.
  3. NH Select Trefferquote mit Kursentwicklung — Wie hat sich der Kurs nach jedem Pick tatsächlich entwickelt? +12% nach 2 Wochen, -5% nach 1 Woche etc. Schließt die
  Feedback-Schleife und macht die KI-Qualität messbar. Das check-outcomes-Cron ist schon da, braucht nur Frontend.

  Mittelfristig interessant:

  4. Konkurrenten-Vergleich — Auf der Asset-Detailseite: 2-3 direkte Wettbewerber mit Score und Kursveränderung. Hilft bei relativer Bewertung ohne externes Tool.
  5. Sektor-Radar — Welche Sektoren sind gerade stark/schwach? Gibt Makro-Kontext zu den Picks.

  Meine Empfehlung: Zuerst den Score-Trend — er nutzt vorhandene Infrastruktur, erfordert keine neue KI-Logik und macht das Herzstück der App (den Score) deutlich wertvoller.
   Danach das Morgen-Briefing, weil es die App vom passiven Tool zum aktiven Assistenten macht.


Status 2026-05-25:

Technischer Stand:
- git status war sauber.
- npm test erfolgreich: 21/21 Tests gruen.
- npm run build erfolgreich mit Next.js 16.2.6.
- npm run lint ist aktuell kaputt, weil "next lint" unter Next 16 nicht mehr sauber passt. Script auf "eslint ." umstellen.

Aktueller App-Zustand:
- App ist deutlich ueber MVP hinaus: Watchlist, Detailseiten, Portfolio, Alerts, Screener, Marktuebersicht, News, NH Select, Cronjobs, Push, KI-Pipeline.
- KI-Pipeline: Opus orchestriert, Felix/Fundamental, Nina/Sentiment, Marco/Market Intelligence, Vera/Fact-Check, Kai/Vergleich, Radar/Trending, Scouts, Synthesizer.
- Vera laeuft im Hauptpfad nach complete_analysis, prueft mit Excerpts und kann bis zu 3 whitelisted Artikel via fetch_article nachladen.
- Vera-Findings koennen als Guardrails fuer spaetere Opus-Laeufe genutzt werden.

Wichtigste offene Punkte:
1. Supabase-Schema aktualisieren:
   supabase/schema.sql enthaelt nur die urspruenglichen Tabellen. Code nutzt inzwischen zusaetzlich ai_analyses, fact_check_findings, portfolio_positions, price_alerts, nh_select_daily, radar_signals, analysis_outcomes, push_subscriptions, morning_briefings usw.

2. .env.local.example aktualisieren:
   Fehlen u.a. ANTHROPIC_API_KEY, CRON_SECRET, NEXT_PUBLIC_APP_URL, JINA_API_KEY, VAPID Keys, RESEND_API_KEY, ALERT_FROM_EMAIL.

3. Oeffentliche API-Routen pruefen:
   /api/nh-select und /api/nh-select/history sind ohne Auth erreichbar. history nutzt Service Role. Entscheiden: bewusst oeffentlich oder absichern.

4. Supabase-Typisierung aufraeumen:
   Viele "(supabase as any)"-Stellen durch unvollstaendige DB-Typen/Schema-Drift. Nach Schema-Update Typen nachziehen.

5. Tests ausbauen:
   Aktuell nur Scoring/Validation. Naechste Tests fuer API-Routen, AI-Output-Schema, Vera/Guardrails, Cronjobs, Portfolio/Alerts.

Empfohlene Reihenfolge:
1. Supabase-Migration/schema.sql komplett machen.
2. Env-Beispiel + README Deployment/Secrets aktualisieren.
3. Lint-Script fixen.
4. Oeffentliche Routen bewusst entscheiden.
5. Danach erst neue Features/Agenten.

Backlog 2026-05-27:

Investigate AVGO data ingestion quality:
- Warum fehlt KGV?
- Warum fehlen EDGAR-Quartalsdaten?
- Warum fehlt strukturierter Analystenkonsens?
- Warum fehlt Debt/Equity?
- Betrifft das nur AVGO oder mehrere Large Caps?

Hinweis: Solche Luecken als Provider-/Ingestion-Limitation behandeln, nicht als operatives Unternehmensrisiko.
